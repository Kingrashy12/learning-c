#include <stdio.h>

int main() {
    int x = 10;
    int y = 3;
    int z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y; 
    // z = x % y;
    // x++;
    // x--;
    // x+=3
    // x-=3
    // x*=3;
    // x/=3;


    printf("%d\n", z);

    return 0;
}